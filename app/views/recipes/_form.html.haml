= simple_form_for(@recipe) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :description
    = f.input :image_url
    = f.association :user, as: :hidden

  .form-actions
    = f.button :submit
